# 复制为 .env（已 gitignore），本地开发用。
# 环境变量会覆盖 config.yaml 中的同名字段。

# 数据库
SCP_DATABASE_POSTGRES_DSN=postgres://scp:scp_pwd@127.0.0.1:5432/smart_control?sslmode=disable

# AI 云端 fallback（Task 7 阶段启用）
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
